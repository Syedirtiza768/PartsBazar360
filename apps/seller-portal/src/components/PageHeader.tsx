/**
 * The page header and metric tile now live in the shared design system so the
 * seller, admin and operations portals stay in step. Re-exported here to keep
 * existing call sites working.
 */
export { PageHeader, StatCard, StatGrid } from "@repo/ui/page-header";
